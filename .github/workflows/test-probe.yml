name: Test Probe
on:
  schedule:
    - cron: '8 16 * * *'
  workflow_dispatch:
jobs:
  probe:
    runs-on: ubuntu-latest
    steps:
      - name: 检出代码
        uses: actions/checkout@v5
      - name: hello
        run: echo ok
